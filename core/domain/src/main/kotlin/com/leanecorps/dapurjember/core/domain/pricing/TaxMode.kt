package com.leanecorps.dapurjember.core.domain.pricing

/** Whether menu prices already contain tax ([INCLUSIVE]) or have it added on top ([EXCLUSIVE]). */
enum class TaxMode { EXCLUSIVE, INCLUSIVE }
